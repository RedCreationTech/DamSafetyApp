# DamSafetyApp 部署运行说明

> 文档版本：1.0
>
> 适用平台：Windows 10/11 + WSL2（推荐 Ubuntu），以及其他已有 MOOSE 开发环境的 Linux 机器
>
> 当前验证基线：MOOSE `4bce02d91b56c7ed845a5747df4d24f415592504`、`moose-dev=2026.07.30=mpich`

## 1. 适用范围

本文说明如何在另一台 Windows 电脑上从源码构建并直接运行 DamSafetyApp，不依赖
`192.168.0.138`、`/home/kevin/DamSafetyApp` 或该计算机上的既有二进制。

- Windows 上使用 WSL2 内的 Linux 终端完成克隆、编译、测试和求解；不要在
  PowerShell、CMD 或原生 Windows Conda 中直接编译。
- 构建产物是 WSL/Linux 可执行文件 `DamSafetyApp-opt`，不是 Windows `.exe`。
- 本流程适用于开发、调试和人工运行。直接命令产生的结果不是 LIMS Job，不会获得
  `job_id`；如需接入 C06 Agent 和 LIMS 正式作业链路，还需另行部署 Agent、鉴权和
  LIMS 地址配置。

MOOSE 官方要求开发环境为 POSIX 类 Unix 系统；Windows 应使用 WSL。参考：

- [MOOSE 安装入口](https://mooseframework.inl.gov/getting_started/installation/index.html)
- [MOOSE Conda 开发环境](https://mooseframework.inl.gov/getting_started/installation/conda.html)
- [MOOSE 构建系统](https://mooseframework.inl.gov/application_development/build_system.html)

## 2. 前置条件

### 2.1 Windows 和 WSL2

先在 PowerShell 中确认 WSL 发行版使用版本 2：

```powershell
wsl --status
wsl --list --verbose
wsl --distribution Ubuntu
```

后续所有命令均在刚打开的 Ubuntu/WSL Bash 终端内运行。建议把源码放在 WSL 的
Linux 文件系统（例如 `~/projects`）中，不要放在 `/mnt/c` 下，以免编译变慢或遇到
权限、符号链接及换行符问题。

### 2.2 MOOSE 开发环境

“已有 MOOSE 环境”必须同时具备：

1. 可编译 MOOSE 应用的依赖环境，例如 Conda 的 `moose-dev`；
2. 完整的 MOOSE 源码树，其中存在 `framework/Makefile`；
3. `make`、C++ 编译器、Python 和 MPI。

只有预编译的 `moose-opt` 命令不足以编译 DamSafetyApp。推荐使用当前验证过的组合：

```text
MOOSE commit: 4bce02d91b56c7ed845a5747df4d24f415592504
Conda package: moose-dev=2026.07.30=mpich
```

若现有 MOOSE 不是该提交，仍可尝试构建，但属于未验证组合；必须重新执行本文的全部
测试，不能直接视为与当前验证基线等价。

## 3. 获取代码并设置路径

在 WSL 中执行：

```bash
mkdir -p ~/projects
cd ~/projects
git clone git@github.com:RedCreationTech/DamSafetyApp.git
cd DamSafetyApp
git checkout main
```

如交付方指定了分支、标签或提交 SHA，应使用指定版本替换 `main`。记录实际版本：

```bash
git rev-parse HEAD
```

激活已有的 MOOSE 开发环境，并设置两个本机路径。以下假设 Conda 环境名为 `moose`、
MOOSE 源码位于 `~/projects/moose`；路径不同时按实际位置修改：

```bash
conda activate moose
export DAMSAFETYAPP_DIR="$HOME/projects/DamSafetyApp"
export MOOSE_DIR="$HOME/projects/moose"
```

检查环境：

```bash
test -f "$MOOSE_DIR/framework/Makefile"
git -C "$MOOSE_DIR" rev-parse HEAD
which python
which mpicxx
which mpiexec
```

第一条命令无输出且退出码为 0 表示 `MOOSE_DIR` 结构正确。`git rev-parse` 推荐输出：

```text
4bce02d91b56c7ed845a5747df4d24f415592504
```

## 4. 编译 DamSafetyApp

在已激活 MOOSE 环境的同一个 WSL 终端中执行：

```bash
cd "$DAMSAFETYAPP_DIR"
MOOSE_DIR="$MOOSE_DIR" METHOD=opt make -j4
```

- `-j4` 表示最多使用 4 个并行编译任务，可按 CPU 和内存调整；首次部署建议从 4 开始。
- `METHOD=opt` 生成用于正常计算的优化版本。
- 日常开发可改为 `METHOD=devel`，调试器场景可改为 `METHOD=dbg`，但二者运行速度更慢。

成功后应生成：

```bash
ls -lh "$DAMSAFETYAPP_DIR/DamSafetyApp-opt"
"$DAMSAFETYAPP_DIR/DamSafetyApp-opt" --version
"$DAMSAFETYAPP_DIR/DamSafetyApp-opt" --help
```

每次修改 C++ 源码或切换 MOOSE 版本后，都应重新执行 `make`，并重新运行测试。

## 5. 首次部署验证

### 5.1 输入语法检查

先使用项目自带的单单元弹性输入做无求解语法检查：

```bash
cd "$DAMSAFETYAPP_DIR/test/tests/smoke"
"$DAMSAFETYAPP_DIR/DamSafetyApp-opt" --check-input -i elastic.i
```

命令应以退出码 0 结束，且不出现 `ERROR`。

### 5.2 执行最小算例

MOOSE 输入常使用相对路径，因此应先进入 `.i` 文件所在目录再运行：

```bash
cd "$DAMSAFETYAPP_DIR/test/tests/smoke"
"$DAMSAFETYAPP_DIR/DamSafetyApp-opt" -i elastic.i
```

运行结束后可查看本目录新生成的 Exodus、CSV 或其他输出：

```bash
find . -maxdepth 1 -type f -newer elastic.i -print
```

### 5.3 执行项目回归测试

```bash
cd "$DAMSAFETYAPP_DIR"
MOOSE_DIR="$MOOSE_DIR" METHOD=opt ./run_tests -j2
```

验收条件是测试汇总中 `failed` 为 0。跳过项需要结合测试标签判断，不能把 `skipped`
自动解释为通过。

需要同时验证 C++ 单元测试时执行：

```bash
cd "$DAMSAFETYAPP_DIR"
MOOSE_DIR="$MOOSE_DIR" METHOD=opt make -C unit -j4
MOOSE_DIR="$MOOSE_DIR" METHOD=opt ./unit/run_tests
```

## 6. 运行自己的输入文件

假设输入文件为 `/home/yourname/cases/dam/case.i`：

### 6.1 串行运行

```bash
cd /home/yourname/cases/dam
"$DAMSAFETYAPP_DIR/DamSafetyApp-opt" --check-input -i case.i
"$DAMSAFETYAPP_DIR/DamSafetyApp-opt" -i case.i
```

### 6.2 本机 MPI 并行运行

```bash
cd /home/yourname/cases/dam
mpiexec -n 2 "$DAMSAFETYAPP_DIR/DamSafetyApp-opt" -i case.i
```

`-n 2` 表示使用 2 个 MPI rank。首次运行先使用 1～2 个 rank，确认正确后再按机器的
CPU、内存和算例规模增加，不要超过可用资源。

### 6.3 保留运行记录

建议每个算例使用独立目录，并保存求解日志和版本信息：

```bash
cd /home/yourname/cases/dam
git -C "$DAMSAFETYAPP_DIR" rev-parse HEAD > DamSafetyApp.commit.txt
git -C "$MOOSE_DIR" rev-parse HEAD > MOOSE.commit.txt
mpiexec -n 2 "$DAMSAFETYAPP_DIR/DamSafetyApp-opt" -i case.i 2>&1 | tee run.log
```

## 7. 当前项目脚本与可移植性说明

根 README 中的 `scripts/p0/*.sh` 用于复现既定 BlackBear/MOOSE 基线，
`scripts/cdp/*.sh` 用于既有计算节点上的隔离开发。部分脚本目前默认使用：

```text
/home/kevin/DamSafetyApp
/home/kevin/miniforge3/bin/conda
```

因此在另一台电脑上不要直接把这些脚本当作通用安装器。本文给出的
`MOOSE_DIR=... METHOD=... make`、`./run_tests` 和 `DamSafetyApp-opt -i ...` 是跨机器的
通用入口，也不会访问 `192.168.0.138`。

## 8. 常见问题

### 8.1 找不到 `framework/build.mk` 或 `framework/Makefile`

`MOOSE_DIR` 指错了目录，或只安装了预编译 MOOSE 而没有源码。确认：

```bash
ls "$MOOSE_DIR/framework/Makefile"
```

### 8.2 找不到 `mpicxx`、`mpiexec`、PETSc 或 libMesh

通常是没有激活构建 MOOSE 时使用的开发环境。重新执行 `conda activate <环境名>`，并
确认 MOOSE 源码和 Conda 包版本匹配。

### 8.3 编译成功但运行时出现动态库或 MPI ABI 错误

不要复制 `192.168.0.138` 上的旧二进制到新电脑。应在目标 WSL 环境中用目标机器的
MOOSE/Conda 环境重新编译，并在运行前激活同一个环境。

### 8.4 输入文件能找到，但 CSV、网格或 include 文件找不到

先 `cd` 到主 `.i` 文件所在目录再运行。检查输入中的相对路径是否也以该目录为基准。

### 8.5 WSL 中编译很慢或脚本权限异常

把仓库和算例移到 `~/projects`、`~/cases` 等 WSL Linux 文件系统路径后重新构建；避免
从 `/mnt/c` 直接编译。代码应在 WSL 内用 Git 克隆，不要从 Windows 文件夹手工复制。

## 9. 部署完成检查表

- [ ] DamSafetyApp 和 MOOSE 的提交 SHA 已记录；
- [ ] MOOSE 开发环境已激活，`MOOSE_DIR/framework/Makefile` 存在；
- [ ] `DamSafetyApp-opt` 在目标 WSL 机器上重新编译生成；
- [ ] `--check-input` 成功；
- [ ] 最小弹性算例成功；
- [ ] 项目回归测试 `failed = 0`；
- [ ] MPI 运行（如需）已用受控 rank 数验证；
- [ ] 自有算例、输入依赖、日志和结果位于独立目录；
- [ ] 若用于正式平台作业，已另行完成 C06 Agent/LIMS 接入并取得服务端 `job_id`。
