# 原结构压缩失败状态回归

`TEST-CDPC-BOUND-001`，来自 LIMS `job_20260831_221403_qh908c`，诊断源码 `3f5a4b8`。

`failed_states.json` 保存 16 个局部失败状态与 13 个完整材料失败调用，以及原 JSONL 来源和 SHA-256。它们来自求解器实际试算，未必是全局接受状态；不是实验真值，也不代表 Abaqus 参考。

四表直接复用 `../uniaxial_tension_20260830/`；已与该压缩 Job 输入逐字节核对，四个文件的 SHA 记录在 JSON 中。E=2.97915e10 Pa、ν=0.2、ψ=36°、e=0.1、fb0/fc0=1.16、Kc=0.667，μ=0.0005 s、wt=0、wc=1。没有为了回归修改材料参数。

局部未知量 0～5 是按应力尺度归一化的骨架应力，6 是塑性乘子，7/8 是本子步拉/压历史增量（按应变尺度归一化），不是累计损伤。旧状态、目标应变、AD/FD 矩阵均保留用于重复验证。
